# Python依赖：PySpark 3.4.0(必须与镜像JVM版本一致) + Pandas/NumPy(数据生成) + PyYAML(配置) + Tabulate(表格) + psycopg2(PG驱动) + SciPy(统计)
pyspark==3.4.0
pandas==2.1.1
numpy==1.26.0
pyyaml==6.0.1
tabulate==0.9.0
psycopg2-binary==2.9.9
scipy==1.11.3
